[project]
name = "rpi-toolkit"
version = "0.2.0"
description = "Raspberry Pi 5 monitoring toolkit with interactive TUI"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.80.0",
    "requests>=2.28.0",
]
